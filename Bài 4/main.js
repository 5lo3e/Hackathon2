let arr=prompt("Nhập chuỗi ký tự cần kiểm tra");
console.log("2"/1);
let result=[];
let newArr=arr.split("");
console.log(arr.split(""));
for (let i = 0; i < newArr.length; i++) {
    if (isNaN(newArr[i]/1)) {
        result.push(newArr[i]);
    }
};
console.log(result.join(""));

